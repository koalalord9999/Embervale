import { Monster } from '@/types';

export const armored: Monster[] = [
    // This file is for monsters whose PRIMARY type is Armored.
    // Currently, all armored monsters have another type (e.g., Humanoid) listed first.
];
